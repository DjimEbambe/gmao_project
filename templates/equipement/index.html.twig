{% extends 'base.html.twig' %}

{% block title %}Hello EquipementController!{% endblock %}

{% block content %}
    <div class="container">
        <div class="container">
            <h1 class="mt-2">Liste des équipements</h1>
            <table class="table table-striped table-bordered table-sm">
                <thead>
                <tr>
                    <th>Identifiant</th>
                    <th>Nom</th>
                    <th>Ligne</th>
                </tr>
                </thead>
                <tbody>
                {% for intervation in equipement %}
                    <tr>
                        <td>{{ intervation.id}}</td>
                        <td>{{ intervation.name }}</td>
                        <td>{{ intervation.ligne }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="7">no records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

        </div>
    </div>
{% endblock %}
